@using Application.Helpers;
@using Domain.Common.Attributes;
@{
    ViewData["Title"] = "Yönetici Listesi";
}
@model IEnumerable<Application.Admin.Models.Admin.AdminIndexModel>
<div class="content-wrapper">
    <div class="row">
        <div class="col-lg-12 grid-margin stretch-card">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Yönetici Listesi</h4>
                    <div class="table-responsive">
                        @if (Model != null && Model.Count() > 0)
                        {
                            <table class="table">
                                <thead>
                                    <tr>
                                        <th>Kullanıcı Adı</th>
                                        <th>Oluşturma Tarihi</th>
                                        <th>İşlemler</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var admin in Model.OrderByDescending(k => k.CreatedTime))
                                    {
                                        <tr>
                                            <td>@admin.UserName</td>
                                            <td>@(admin.CreatedTime.ToShortDateString()) </td>
                                            <td>
                                                <button data-id="@admin.Id" type="button" class="btn btn-danger button-sm deleteButton">Sil</button>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        }
                        else
                        {
                            <p>Kayıtlı yönetici bulunmamaktadır.</p>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
@section Scripts{
    <script type="text/javascript" src="~/Scripts/Admin/index.js" asp-append-version="true"></script>
}

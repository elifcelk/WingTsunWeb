@{
    ViewData["Title"] = "Galeri Listesi";
}
@model IEnumerable<Application.Admin.Models.Gallery.GalleryViewModel>
<div class="content-wrapper">
    <div class="row">
        <div class="col-lg-12 grid-margin stretch-card">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Galeri Listesi</h4>
                    <div class="table-responsive">
                        @if (Model.Count() > 0)
                        {
                            <table class="table">
                                <thead>
                                    <tr>
                                        <th>Resim</th>
                                        <th>Durum</th>
                                        <th>İşlemler</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var gallery in Model)
                                    {
                                        <tr>
                                            <td><img src="https://localhost:7006/@gallery.PhotoPath" style="height:200px;width:200px;" /></td>
                                            <td>
                                                @if (gallery.IsDeleted == true)
                                                {
                                                    <label class="badge badge-danger">Pasif</label>
                                                }
                                                else
                                                {
                                                    <label class="badge badge-success">Aktif</label>
                                                }
                                            </td>
                                            
                                            <td>
                                                @if (gallery.IsDeleted == true)
                                                {
                                                    <button data-id="@gallery.Id" type="button" class="btn btn-success button-sm changeStatus">Aktif Yap</button>
                                                }
                                                else
                                                {
                                                    <button data-id="@gallery.Id" type="button" class="btn btn-danger button-sm changeStatus">Pasif Yap</button>
                                                }
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        }
                        else
                        {
                            <p>Kayıtlı resim bulunmamaktadır.</p>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
@section Scripts{
    <script type="text/javascript" src="~/Scripts/Gallery/index.js" asp-append-version="true"></script>
}
